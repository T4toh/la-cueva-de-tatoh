import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tatoh.comidas',
  appName: 'Comidas',
  webDir: 'dist/comidas/browser'
};

export default config;
