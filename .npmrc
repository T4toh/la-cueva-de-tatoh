# Configuración de pnpm
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
# Corta el install si el Node no le sirve a Angular, en vez de dejar que
# `ng build` falle mucho más tarde con un error que no dice qué hacer.
engine-strict=true
